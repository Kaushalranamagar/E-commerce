//---Array---////
// let arr = new array(1,2,3)


//let arr=[0,1,2] indexes

//let arr = [1, 2, 3, 4, 5]
//arr.push(10); //=> to add  the last variable
//arr.shift(); //=> to remove the first variable
//arr.unshift(12); //=> to insert the first variables
//arr.pop() //=> to remove the last variables
//arr.splice(1, 2, 6, 8) //=> to delete the middle variables through indexes and add the variables


//let new_arr = arr.slice(1, 4)

//console.log({ arr })
//console.log({ new_arr })


//let arr2 = []

//for (let i = 1; i < 5; i++) {
//  arr2.push(i)
//}
//console.log({ arr2 })

//let arr3 = [1, 2, 3, 4, 5, 6]

//higher order function

//let filterted_arr3 = arr3.filter((element) => { return element < 3 })
//let lessthanthree = (el) => { return el < 3 }
//let filtered_arr = arr3.filter((element) => element < 2)
//let filtered_arr = arr3.filter(lessthanthree)

//console.log({ filterted_arr3 })
//console.log({ filtered_arr })

//console.log(arr.length)
//let arr3 = [1, 2, 3, 4, 5]

//for (let i = 0; i < arr3.length; i++) {
//  arr3[i] = arr3[i] * 2
//}
//-------or------//
//arr3.forEach((el, index) => {
// el = el * 2;
//  arr3[index] = el * 2

//})

//et mapped_arr = arr3.map((el, index) => el * index)


//console.log(arr3)
//console.log(mapped_arr)